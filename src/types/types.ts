export type Lunchbox = {
  id: string,
  user_id: string,
  name: string,
  favorite: boolean
}

export type Food = {
  id: string,
  name: string,
  description: string,
  image: string,
  category: string,
}